apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ${operator}
  namespace: ${namespace}
spec:
  channel: ${channel}
  installPlanApproval: Automatic
  name: ${operator}
  source: ${source}
  sourceNamespace: ${source_namespace}
  startingCSV: ${operator}.v${version}
  config:
    nodeSelector:
      node-role.kubernetes.io/infra: ""
