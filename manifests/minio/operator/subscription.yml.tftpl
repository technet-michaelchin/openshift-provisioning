apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: minio-operator
  namespace: ${namespace}
spec:
  channel: ${channel}
  installPlanApproval: Automatic
  name: minio-operator
  source: certified-operators
  sourceNamespace: openshift-marketplace
  startingCSV: minio-operator.v${version}
  config:
    nodeSelector:
      node-role.kubernetes.io/infra: ""
